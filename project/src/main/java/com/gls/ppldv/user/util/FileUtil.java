package com.gls.ppldv.user.util;

public class FileUtil {
	
	/*
	public static MultipartFile convertToMultipartFile(MultipartHttpServletRequest request) {
		MultipartFile file = null;
		
		try {
			// 멀티파트 요청에서 파일 데이터를 추출하기 위해 iterator를 사용
			Iterator<String> fileNames = request.getFileNames();
			while (fileNames.hasNext()) {
				String fileName = fileNames.next();
				// 요청에서 파일 이름을 사용해 파일 데이터 가져오기
				Map<String, MultipartFile> fileMap = request.getFileMap();
				file = fileMap.get(fileMap);
			}
		} catch (Exception e) {
			e.printStackTrace();
		}
		return file;
	}
	*/
	
}
